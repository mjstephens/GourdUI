using GourdUI;
using UnityEngine;

/// <summary>
/// UIScreen implementation.
/// </summary>
public class #CLASSNAME#: 
    UIScreen <#CONTRACTSCREEN#, #CONTRACTVIEW#, #STATE#>, 
    #CONTRACTSCREEN#
{
    #region Setup

    public override void OnScreenInstantiated()
    {
        base.OnScreenInstantiated();
        
        // Opportunity to establish state from external sources before view is updated
    }

    #endregion Setup


    #region State

    protected override void ResetScreenState()
    {
        // Use "state" variable to access #STATE#
    }
    
    protected override void OnViewPreDestroy()
    {
        // This is called just before a view is destroyed to be swapped with a new view.
        // You can grab any values from the view you may need for state data (position, scale, etc)
    }

    #endregion State
}